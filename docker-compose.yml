services:
  ckb-exporter:
    # Option A: Build locally
    build: .
    # Option B: Use pre-built image from GHCR (uncomment below, comment out 'build' above)
    # image: ghcr.io/jiangxianliang007/ckb-node-monitor:latest
    env_file: .env
    ports:
      # Host port:Container port — change the host port (left) as needed
      # Container always listens on 8090
      - "8090:8090"
    restart: unless-stopped

  # Optional local Prometheus for standalone monitoring
  # prometheus:
  #   image: prom/prometheus:latest
  #   volumes:
  #     - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
  #   ports:
  #     - "9090:9090"
