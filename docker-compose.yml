services:
  ckb-exporter:
    build: .
    env_file: .env
    ports:
      - "8090:8090"

  # Optional local Prometheus for standalone monitoring
  # prometheus:
  #   image: prom/prometheus:latest
  #   volumes:
  #     - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
  #   ports:
  #     - "9090:9090"
